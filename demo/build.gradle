//add everything here
group = 'com.epam'
version = '1.3.5'

repositories {
    mavenCentral()
}
dependencies {
    implementation group: 'com.task', name: 'utils', version: '1.3.5'
}
test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')